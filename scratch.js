var model 

    //create empty array
    let array = []

    //concatenate number values until operator occurs, then break

    //count # of values in array
    for (i = 0; i <= array.length; i++){
        if (i = 4) {
            //perform equation
        } 
    }